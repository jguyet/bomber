# Bomber Game Server — Environment Variables
# Copy this file to .env and adjust as needed
# cp .env.example .env

# ─── Server ───────────────────────────────────────────────────────────────────

# Port the unified HTTP + WebSocket server listens on
PORT=8060

# Node environment: production | development
NODE_ENV=production
